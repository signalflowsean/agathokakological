'use strict'

const fileSaver = (() => { 
  const createFolder = (folderName) => { 
    // makeDir(folderName); 
  }; 

  const startRecording = () => { 

  }; 

  const stopRecording = () => { 

  }; 

  return { createFolder, startRecording, stopRecording }

})(); 